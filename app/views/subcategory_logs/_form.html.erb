<%= form_with(model: subcategory_log, local: true) do |form| %>
  <% if subcategory_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcategory_log.errors.count, "error") %> prohibited this subcategory_log from being saved:</h2>

      <ul>
      <% subcategory_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subcategory_id %>
    <%= form.number_field :subcategory_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
